# PROGRESS.md

프로젝트 진행 상황 기록. 의미 있는 진행(설정 완료, 실험 결과, 중요한 결정)이 생길 때마다 날짜와 함께 추가한다.

## 2026-09-06

- **환경 세팅**: `venv`에 Python 3.14 + PyTorch 2.14.0+cu126 설치 확인, CUDA GPU 사용 가능 확인.
- **베이스라인 스크립트 작성**: `models/vgg16_cifar10_baseline.py` 추가.
  - CIFAR-10(32x32)에 맞게 조정한 VGG16 (5단계 conv+BN+ReLU+maxpool, 3-layer FC 분류기).
  - 방어 기법 없이 순수 SGD(momentum, weight decay, cosine LR)로 학습.
  - 학습 종료 후 최종 test accuracy 출력, 가중치를 `results/`에 저장.
  - 추후 SVD/RSR/quantization 방어 기법 적용 버전과 비교할 undefended baseline 역할.
- **1 에폭 테스트 실행 결과**: 스크립트 정상 동작 및 GPU 사용 확인.
  - device: `cuda`
  - train_loss=2.1512, train_acc=15.68% (1 epoch, 60.1초)
  - 최종 test accuracy: 20.05%
  - 체크포인트 저장 확인: `results/vgg16_cifar10_baseline.pth`
  - 에러 없이 exit code 0으로 종료.
- **Git**: 베이스라인 스크립트 커밋 및 GitHub(`origin/main`)에 푸시 완료.
- **3 에폭 테스트**: 에폭당 약 61~79초, test accuracy 62.46% (3 epoch만).
- **체크포인트/best-model 기능 추가**: `models/vgg16_cifar10_baseline.py`에 10에폭마다 체크포인트 저장, test accuracy가 갱신될 때마다 `results/best_model.pth`로 별도 저장하는 기능 추가.
- **100 에폭 전체 학습 1차 시도 실패**: 백그라운드 실행 중 약 62~63 에폭 부근에서 시스템 메모리 부족으로 프로세스가 강제 종료됨(exit code 1). 원인 추정: 매 에폭마다 DataLoader worker(`num_workers=4`)를 재생성하는 구조가 Windows 환경에서 장시간 실행 시 메모리를 서서히 누적시킴. 그 시점까지 `checkpoint_epoch60.pth`와 `best_model.pth`(test accuracy 88.74%)는 정상 저장되어 있었음.
- **`--resume` 기능 추가 및 메모리 완화 후 재개**: `num_workers` 기본값을 2로 낮추고 `persistent_workers=True` 적용, 체크포인트에 optimizer/scheduler 상태까지 포함해 저장하도록 확장, `--resume`/`--start-epoch`/`--initial-best-acc` 옵션으로 기존 체크포인트에서 이어서 학습 가능하게 함. CosineAnnealingLR을 이어서 재현할 때는 `last_epoch` 파라미터로 바로 점프하면 잘못된 LR이 나온다는 것을 스모크 테스트로 확인해, `scheduler.step()`을 이어받은 에폭 수만큼 재생(replay)하는 방식으로 수정함.
- **100 에폭 완료** (`checkpoint_epoch60.pth`에서 61~100 에폭 이어 학습):
  - epoch 100 시점 test accuracy: **93.80%**
  - 100 에폭 중 최고 기록(`best_model.pth`, 약 97 에폭 부근): **93.83%**
  - 순수 학습 시간 합계 약 3시간 35분 (1차 실행 60에폭 ≈ 2시간 23분 + 재개 40에폭 ≈ 1시간 12분), 최초 시작부터 완료까지 전체 경과 약 3시간 56분.
  - 재개 실행도 완료 직후(에폭 100 체크포인트 저장 후) 한 번 더 메모리 부족으로 프로세스가 죽었으나, 그 시점엔 이미 모든 결과가 저장된 뒤라 실질적 손실 없음. 즉 메모리 누수는 완화됐을 뿐 완전히 해결되진 않음 — 더 긴 학습을 돌릴 경우 `num_workers=0`으로 더 낮추거나 근본 원인을 추가로 조사할 필요 있음.
  - 결과물: `results/vgg16_cifar10_baseline.pth`(최종), `results/best_model.pth`(최고 기록), `results/checkpoints/checkpoint_epoch{10..100}.pth`(10에폭 단위 전체 이력).
- **RSR (SVD 기반 랜덤 재구성) 방어 구현**: `models/rsr.py` 추가.
  - 채널별 SVD 분해 후, 특이값 크기에 비례한 확률로 성분을 비복원추출하여 재구성 → R=10회 독립 시행 → 분류 결과 다수결(majority vote)로 최종 예측.
  - 학습된 `vgg16_cifar10_baseline.pth`(무방어 베이스라인, 93.80%)를 로드해 공격 없는 상태로 테스트셋 10,000장 전체 평가.
  - 무방어 forward pass를 이 스크립트에서 재현해도 93.80%로 일치(비교 신뢰성 확인).
  - **RSR 방어 적용 clean accuracy: 92.55%** (keep_ratio=0.5) → 베이스라인 대비 **-1.25%p**. 랜덤 재구성 기반 방어의 일반적인 클린 정확도 트레이드오프로 판단됨.
  - 공격 상황에서의 강건성은 `attacks/` 코드 준비 후 별도 측정 필요.
- **특이값 양자화(버킷팅) 구현 및 버그 발견/수정**: `models/quantization.py` 추가.
  - **1차 시도(누적 에너지 균등분할)**: 특이값 에너지(σ², Frobenius norm 기여도)의 누적 비율을 K=5 구간(0-20%, 20-40%, ..., 80-100%)으로 등분해 버킷을 나누는 방식으로 구현.
  - **문제 발견**: 테스트 이미지 5장으로 확인해보니 5장 모두 버킷0~3이 완전히 비고 32개 성분 전부가 버킷4(80-100%)로 몰림. 원인 분석 결과, raw 픽셀 공간(RSR과 동일하게 정규화 전 [0,1])에서 SVD한 이미지는 거의 일정한 배경/평균 밝기에 해당하는 첫 번째(가장 큰) 특이값 하나가 전체 σ² 에너지의 약 90~99%를 이미 차지함 — 첫 성분의 누적 비율이 곧바로 80% 문턱을 넘어버려 버킷0~3에 도달하는 성분이 하나도 없게 됨. 코드 버그가 아니라 자연 이미지 SVD 스펙트럼의 전형적 특성(에너지가 극도로 소수 성분에 집중)임을 확인.
  - **해결(2차 시도, 성분 개수 균등분할)**: 버킷 기준을 누적 에너지 대신 "정렬된 특이값의 개수"로 변경 — `quantize_by_component_count(k, K)`가 32개 성분을 [7,7,6,6,6]처럼 그룹당 6~7개씩 고정 크기로 나눔 (그룹 크기가 데이터 값과 무관하게 항상 균등/비어있지 않음이 보장됨). 각 그룹의 에너지 비중은 그대로 계산해서 보여줌.
  - **재검증 결과** (5개 이미지, K=5, 그룹크기=[7,7,6,6,6]): 버킷0(상위 7개 성분)이 평균 **99.5%** 에너지를 차지, 버킷1은 **0.4%**, 버킷2~4는 사실상 0%에 수렴. 즉 그룹은 항상 균등하게 채워지되(구조적으로 비어있는 버킷 없음), 에너지가 여전히 소수 상위 성분에 압도적으로 집중된다는 실제 특성은 그대로 관찰 가능해짐.
  - 이전(누적 에너지 방식) 함수 `quantize_by_cumulative_energy`는 원인 설명용으로 코드에 남겨둠(module docstring에 버그 원인과 수정 이유 기록).
- **그룹별 포함 확률 설계**: `models/quantization.py`에 `design_group_probabilities()` 추가.
  - 기본 확률 p_b = 그룹별 평균 에너지 비중(5개 이미지 평균), floor=0.05 적용해 p_b' = max(p_b, floor)로 조정. 합이 1일 필요 없는 독립적인 "포함 확률"로 설계(에너지가 0에 가까운 그룹도 최소 5% 확률 보장).
  - 5개 이미지 결과: p_b = [0.9947, 0.0045, 0.0006, 0.0001, 0.0000] → floor 적용 후 p_b' = [0.9947, 0.05, 0.05, 0.05, 0.05]. 버킷0만 원래 값 유지, 나머지 4개 그룹은 전부 floor로 올라감.
- **"제안 방식" 구현 및 클린 정확도 비교**: `models/proposed.py` 추가. 양자화(K=5 성분개수 그룹) + 그룹별 포함 확률(p_b', floor=0.05)을 결합.
  - 방식: 학습셋 500장으로 p_b/p_b' 사전 보정(테스트셋과 분리해 데이터 누수 방지) → 추론 시 R=10회 반복, 매회 5개 그룹 각각 독립 베르누이(p_b') 시행으로 포함 여부 결정(전부 제외되면 최고 확률 그룹을 강제 포함) → 포함된 그룹의 성분만으로 이미지 재구성 → VGG16 분류 → 10개 예측 다수결로 최종 클래스 결정.
  - 보정 결과(학습셋 500장): p_b = [0.9944, 0.0049, 0.0006, 0.0001, 0.0000], p_b' = [0.9944, 0.05, 0.05, 0.05, 0.05] — 5개 이미지로 봤던 값과 거의 동일해 안정적인 추정으로 확인.
  - **테스트셋 전체(10,000장) clean accuracy 비교**:
    | 방식 | Accuracy |
    |---|---|
    | 베이스라인 (무방어) | 93.80% |
    | 원본 RSR (특이값 크기 비례 샘플링, keep_ratio=0.5) | 92.55% |
    | 제안 방식 (양자화 그룹 + 베르누이(p_b')) | **80.86%** |
  - 같은 스크립트 실행 내에서 베이스라인·원본 RSR도 재계산해 각각 93.80%/92.55%로 기존 결과와 정확히 일치함을 재확인(비교 신뢰성 확보).
  - **원인 분석**: 제안 방식은 베이스라인 대비 -12.94%p, 원본 RSR 대비 -11.69%p로 정확도 하락폭이 큼. p_b'가 버킷0(상위 7개 성분)에는 0.9944, 나머지 4개 그룹에는 각각 0.05만 부여하기 때문에, 대부분의 시행에서 사실상 버킷0의 7개 성분만으로 재구성됨(32개 중 약 22%). 이는 원본 RSR의 keep_ratio=0.5(16개, 약 50%)보다 훨씬 공격적인 저랭크 근사라서 클린 정확도 손실이 더 크게 나타난 것으로 판단됨. 방어력(공격 상황 강건성)은 아직 미측정.
- **floor 값 스윕 실험**: `models/proposed.py`에 평균 keep_ratio(재구성에 실제로 쓰인 성분 비율) 계측 기능 추가, 보정용 학습셋 샘플링에 시드 고정(스윕 간 비교 공정성 확보).
  - floor를 0.05/0.1/0.2/0.3/0.36으로 바꿔가며 테스트셋 전체 재평가:
    | floor | p_b' (버킷1~4) | 평균 keep_ratio | Clean Accuracy | vs 원본 RSR(92.55%) |
    |---|---|---|---|---|
    | 0.05 | 0.05 | 0.2576 | 80.86% | -11.69%p |
    | 0.10 | 0.10 | 0.2965 | 81.92% | -10.63%p |
    | 0.20 | 0.20 | 0.3742 | 85.05% | -7.50%p |
    | 0.30 | 0.30 | 0.4524 | 87.90% | -4.65%p |
    | 0.36 | 0.36 | 0.4995 | 89.21% | -3.34%p |
  - keep_ratio ≈ (7×0.9943 + 25×floor)/32 라는 선형 관계로 예측한 대로, **floor≈0.36에서 평균 keep_ratio(0.4995)가 원본 RSR의 keep_ratio=0.5와 거의 정확히 일치**함을 확인.
  - **흥미로운 발견**: keep_ratio(정보량)를 원본 RSR과 동일하게 맞춰도(0.4995 vs 0.5) clean accuracy는 89.21%로 RSR의 92.55%에 못 미침(-3.34%p 격차 잔존). 즉 평균적으로 사용하는 성분 개수가 같아도 두 방식의 성능 차이가 남음 — 원본 RSR은 성분 하나하나를 크기 비례 확률로 정교하게 선택하는 반면, 제안 방식은 그룹 단위로 한꺼번에 켜고 끄는 거친(coarse-grained) 구조라 매 시행마다 실제 사용되는 구체적 성분의 변동성이 더 크기 때문으로 추정. 그룹 세분화(K를 늘리거나 그룹 크기를 좁히는 등) 개선 여지가 있음.
