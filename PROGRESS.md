# PROGRESS.md

프로젝트 진행 상황 기록. 의미 있는 진행(설정 완료, 실험 결과, 중요한 결정)이 생길 때마다 날짜와 함께 추가한다.

## 2026-09-06

- **환경 세팅**: `venv`에 Python 3.14 + PyTorch 2.14.0+cu126 설치 확인, CUDA GPU 사용 가능 확인.
- **베이스라인 스크립트 작성**: `models/vgg16_cifar10_baseline.py` 추가.
  - CIFAR-10(32x32)에 맞게 조정한 VGG16 (5단계 conv+BN+ReLU+maxpool, 3-layer FC 분류기).
  - 방어 기법 없이 순수 SGD(momentum, weight decay, cosine LR)로 학습.
  - 학습 종료 후 최종 test accuracy 출력, 가중치를 `results/`에 저장.
  - 추후 SVD/RSR/quantization 방어 기법 적용 버전과 비교할 undefended baseline 역할.
- **1 에폭 테스트 실행 결과**: 스크립트 정상 동작 및 GPU 사용 확인.
  - device: `cuda`
  - train_loss=2.1512, train_acc=15.68% (1 epoch, 60.1초)
  - 최종 test accuracy: 20.05%
  - 체크포인트 저장 확인: `results/vgg16_cifar10_baseline.pth`
  - 에러 없이 exit code 0으로 종료.
- **Git**: 베이스라인 스크립트 커밋 및 GitHub(`origin/main`)에 푸시 완료.
- **3 에폭 테스트**: 에폭당 약 61~79초, test accuracy 62.46% (3 epoch만).
- **체크포인트/best-model 기능 추가**: `models/vgg16_cifar10_baseline.py`에 10에폭마다 체크포인트 저장, test accuracy가 갱신될 때마다 `results/best_model.pth`로 별도 저장하는 기능 추가.
- **100 에폭 전체 학습 1차 시도 실패**: 백그라운드 실행 중 약 62~63 에폭 부근에서 시스템 메모리 부족으로 프로세스가 강제 종료됨(exit code 1). 원인 추정: 매 에폭마다 DataLoader worker(`num_workers=4`)를 재생성하는 구조가 Windows 환경에서 장시간 실행 시 메모리를 서서히 누적시킴. 그 시점까지 `checkpoint_epoch60.pth`와 `best_model.pth`(test accuracy 88.74%)는 정상 저장되어 있었음.
- **`--resume` 기능 추가 및 메모리 완화 후 재개**: `num_workers` 기본값을 2로 낮추고 `persistent_workers=True` 적용, 체크포인트에 optimizer/scheduler 상태까지 포함해 저장하도록 확장, `--resume`/`--start-epoch`/`--initial-best-acc` 옵션으로 기존 체크포인트에서 이어서 학습 가능하게 함. CosineAnnealingLR을 이어서 재현할 때는 `last_epoch` 파라미터로 바로 점프하면 잘못된 LR이 나온다는 것을 스모크 테스트로 확인해, `scheduler.step()`을 이어받은 에폭 수만큼 재생(replay)하는 방식으로 수정함.
- **100 에폭 완료** (`checkpoint_epoch60.pth`에서 61~100 에폭 이어 학습):
  - epoch 100 시점 test accuracy: **93.80%**
  - 100 에폭 중 최고 기록(`best_model.pth`, 약 97 에폭 부근): **93.83%**
  - 순수 학습 시간 합계 약 3시간 35분 (1차 실행 60에폭 ≈ 2시간 23분 + 재개 40에폭 ≈ 1시간 12분), 최초 시작부터 완료까지 전체 경과 약 3시간 56분.
  - 재개 실행도 완료 직후(에폭 100 체크포인트 저장 후) 한 번 더 메모리 부족으로 프로세스가 죽었으나, 그 시점엔 이미 모든 결과가 저장된 뒤라 실질적 손실 없음. 즉 메모리 누수는 완화됐을 뿐 완전히 해결되진 않음 — 더 긴 학습을 돌릴 경우 `num_workers=0`으로 더 낮추거나 근본 원인을 추가로 조사할 필요 있음.
  - 결과물: `results/vgg16_cifar10_baseline.pth`(최종), `results/best_model.pth`(최고 기록), `results/checkpoints/checkpoint_epoch{10..100}.pth`(10에폭 단위 전체 이력).
- **RSR (SVD 기반 랜덤 재구성) 방어 구현**: `models/rsr.py` 추가.
  - 채널별 SVD 분해 후, 특이값 크기에 비례한 확률로 성분을 비복원추출하여 재구성 → R=10회 독립 시행 → 분류 결과 다수결(majority vote)로 최종 예측.
  - 학습된 `vgg16_cifar10_baseline.pth`(무방어 베이스라인, 93.80%)를 로드해 공격 없는 상태로 테스트셋 10,000장 전체 평가.
  - 무방어 forward pass를 이 스크립트에서 재현해도 93.80%로 일치(비교 신뢰성 확인).
  - **RSR 방어 적용 clean accuracy: 92.55%** (keep_ratio=0.5) → 베이스라인 대비 **-1.25%p**. 랜덤 재구성 기반 방어의 일반적인 클린 정확도 트레이드오프로 판단됨.
  - 공격 상황에서의 강건성은 `attacks/` 코드 준비 후 별도 측정 필요.
- **특이값 양자화(버킷팅) 구현 및 버그 발견/수정**: `models/quantization.py` 추가.
  - **1차 시도(누적 에너지 균등분할)**: 특이값 에너지(σ², Frobenius norm 기여도)의 누적 비율을 K=5 구간(0-20%, 20-40%, ..., 80-100%)으로 등분해 버킷을 나누는 방식으로 구현.
  - **문제 발견**: 테스트 이미지 5장으로 확인해보니 5장 모두 버킷0~3이 완전히 비고 32개 성분 전부가 버킷4(80-100%)로 몰림. 원인 분석 결과, raw 픽셀 공간(RSR과 동일하게 정규화 전 [0,1])에서 SVD한 이미지는 거의 일정한 배경/평균 밝기에 해당하는 첫 번째(가장 큰) 특이값 하나가 전체 σ² 에너지의 약 90~99%를 이미 차지함 — 첫 성분의 누적 비율이 곧바로 80% 문턱을 넘어버려 버킷0~3에 도달하는 성분이 하나도 없게 됨. 코드 버그가 아니라 자연 이미지 SVD 스펙트럼의 전형적 특성(에너지가 극도로 소수 성분에 집중)임을 확인.
  - **해결(2차 시도, 성분 개수 균등분할)**: 버킷 기준을 누적 에너지 대신 "정렬된 특이값의 개수"로 변경 — `quantize_by_component_count(k, K)`가 32개 성분을 [7,7,6,6,6]처럼 그룹당 6~7개씩 고정 크기로 나눔 (그룹 크기가 데이터 값과 무관하게 항상 균등/비어있지 않음이 보장됨). 각 그룹의 에너지 비중은 그대로 계산해서 보여줌.
  - **재검증 결과** (5개 이미지, K=5, 그룹크기=[7,7,6,6,6]): 버킷0(상위 7개 성분)이 평균 **99.5%** 에너지를 차지, 버킷1은 **0.4%**, 버킷2~4는 사실상 0%에 수렴. 즉 그룹은 항상 균등하게 채워지되(구조적으로 비어있는 버킷 없음), 에너지가 여전히 소수 상위 성분에 압도적으로 집중된다는 실제 특성은 그대로 관찰 가능해짐.
  - 이전(누적 에너지 방식) 함수 `quantize_by_cumulative_energy`는 원인 설명용으로 코드에 남겨둠(module docstring에 버그 원인과 수정 이유 기록).
- **그룹별 포함 확률 설계**: `models/quantization.py`에 `design_group_probabilities()` 추가.
  - 기본 확률 p_b = 그룹별 평균 에너지 비중(5개 이미지 평균), floor=0.05 적용해 p_b' = max(p_b, floor)로 조정. 합이 1일 필요 없는 독립적인 "포함 확률"로 설계(에너지가 0에 가까운 그룹도 최소 5% 확률 보장).
  - 5개 이미지 결과: p_b = [0.9947, 0.0045, 0.0006, 0.0001, 0.0000] → floor 적용 후 p_b' = [0.9947, 0.05, 0.05, 0.05, 0.05]. 버킷0만 원래 값 유지, 나머지 4개 그룹은 전부 floor로 올라감.
- **"제안 방식" 구현 및 클린 정확도 비교**: `models/proposed.py` 추가. 양자화(K=5 성분개수 그룹) + 그룹별 포함 확률(p_b', floor=0.05)을 결합.
  - 방식: 학습셋 500장으로 p_b/p_b' 사전 보정(테스트셋과 분리해 데이터 누수 방지) → 추론 시 R=10회 반복, 매회 5개 그룹 각각 독립 베르누이(p_b') 시행으로 포함 여부 결정(전부 제외되면 최고 확률 그룹을 강제 포함) → 포함된 그룹의 성분만으로 이미지 재구성 → VGG16 분류 → 10개 예측 다수결로 최종 클래스 결정.
  - 보정 결과(학습셋 500장): p_b = [0.9944, 0.0049, 0.0006, 0.0001, 0.0000], p_b' = [0.9944, 0.05, 0.05, 0.05, 0.05] — 5개 이미지로 봤던 값과 거의 동일해 안정적인 추정으로 확인.
  - **테스트셋 전체(10,000장) clean accuracy 비교**:
    | 방식 | Accuracy |
    |---|---|
    | 베이스라인 (무방어) | 93.80% |
    | 원본 RSR (특이값 크기 비례 샘플링, keep_ratio=0.5) | 92.55% |
    | 제안 방식 (양자화 그룹 + 베르누이(p_b')) | **80.86%** |
  - 같은 스크립트 실행 내에서 베이스라인·원본 RSR도 재계산해 각각 93.80%/92.55%로 기존 결과와 정확히 일치함을 재확인(비교 신뢰성 확보).
  - **원인 분석**: 제안 방식은 베이스라인 대비 -12.94%p, 원본 RSR 대비 -11.69%p로 정확도 하락폭이 큼. p_b'가 버킷0(상위 7개 성분)에는 0.9944, 나머지 4개 그룹에는 각각 0.05만 부여하기 때문에, 대부분의 시행에서 사실상 버킷0의 7개 성분만으로 재구성됨(32개 중 약 22%). 이는 원본 RSR의 keep_ratio=0.5(16개, 약 50%)보다 훨씬 공격적인 저랭크 근사라서 클린 정확도 손실이 더 크게 나타난 것으로 판단됨. 방어력(공격 상황 강건성)은 아직 미측정.
- **floor 값 스윕 실험**: `models/proposed.py`에 평균 keep_ratio(재구성에 실제로 쓰인 성분 비율) 계측 기능 추가, 보정용 학습셋 샘플링에 시드 고정(스윕 간 비교 공정성 확보).
  - floor를 0.05/0.1/0.2/0.3/0.36으로 바꿔가며 테스트셋 전체 재평가:
    | floor | p_b' (버킷1~4) | 평균 keep_ratio | Clean Accuracy | vs 원본 RSR(92.55%) |
    |---|---|---|---|---|
    | 0.05 | 0.05 | 0.2576 | 80.86% | -11.69%p |
    | 0.10 | 0.10 | 0.2965 | 81.92% | -10.63%p |
    | 0.20 | 0.20 | 0.3742 | 85.05% | -7.50%p |
    | 0.30 | 0.30 | 0.4524 | 87.90% | -4.65%p |
    | 0.36 | 0.36 | 0.4995 | 89.21% | -3.34%p |
  - keep_ratio ≈ (7×0.9943 + 25×floor)/32 라는 선형 관계로 예측한 대로, **floor≈0.36에서 평균 keep_ratio(0.4995)가 원본 RSR의 keep_ratio=0.5와 거의 정확히 일치**함을 확인.
  - **흥미로운 발견**: keep_ratio(정보량)를 원본 RSR과 동일하게 맞춰도(0.4995 vs 0.5) clean accuracy는 89.21%로 RSR의 92.55%에 못 미침(-3.34%p 격차 잔존). 즉 평균적으로 사용하는 성분 개수가 같아도 두 방식의 성능 차이가 남음 — 원본 RSR은 성분 하나하나를 크기 비례 확률로 정교하게 선택하는 반면, 제안 방식은 그룹 단위로 한꺼번에 켜고 끄는 거친(coarse-grained) 구조라 매 시행마다 실제 사용되는 구체적 성분의 변동성이 더 크기 때문으로 추정. 그룹 세분화(K를 늘리거나 그룹 크기를 좁히는 등) 개선 여지가 있음.
- **K=8로 그룹 세분화 실험 (가설: K를 늘리면 RSR과의 격차가 줄어들 것)**: 32개 성분을 8그룹×4개씩 균등분할 후 동일한 방식으로 floor 탐색.
  - 보정 결과(K=8): p_b0(상위 4개 성분) = 0.9838, 나머지 7개 그룹은 거의 0. keep_ratio 예측식 `(4×0.9838 + 28×floor)/32 = 0.5` → floor≈0.43.
  - 실제 검증: **floor=0.43 → 평균 keep_ratio=0.4996** (예측과 거의 정확히 일치), **clean accuracy=84.70%**, 원본 RSR 대비 **-7.85%p**.
  - **결과: 가설과 반대로 격차가 오히려 커짐** — K=5(floor=0.36)에서는 -3.34%p였는데 K=8(floor=0.43)에서는 -7.85%p로 2배 이상 벌어짐.
    | K | floor | 평균 keep_ratio | Clean Accuracy | vs 원본 RSR(92.55%) |
    |---|---|---|---|---|
    | 5 | 0.36 | 0.4995 | 89.21% | -3.34%p |
    | 8 | 0.43 | 0.4996 | 84.70% | -7.85%p |
  - **원인 추정**: K가 커질수록 "항상 포함되는 핵심 상위 성분(버킷0)" 개수가 줄어듦(7개→4개). 같은 keep_ratio=0.5를 맞추려면 나머지(더 많아진) 그룹들을 더 높은 확률(0.36→0.43)로 켜야 하는데, 이는 매 시행마다 랜덤하게 on/off되는 성분 비중이 늘고 핵심 정보의 결정론적 보존 비율은 줄어든다는 뜻. 즉 그룹 세분화(K 증가)가 정보 손실을 줄여줄 것이라는 가설과 달리, 오히려 재구성의 시행 간 일관성을 떨어뜨려 역효과를 냄. K=5가 K=8보다 더 나은 설계임을 확인.
- **적대적 공격(FGSM/PGD) 구현 및 BPDA 견고성 평가**: `attacks/attacks.py`(공격), `attacks/evaluate.py`(평가) 추가.
  - FGSM: `x' = x + ε·sign(∇ₓL(x,y))`. PGD: T=20회 반복, α=2/255, 매 스텝 ε-경계 L∞ 투영 + [0,1] 클리핑.
  - RSR·제안 방식은 재구성(랜덤 샘플링/이산 마스크)이 미분 불가능하므로 `BPDAIdentity`(커스텀 autograd.Function)로 forward는 실제 재구성을 그대로 실행하고 backward는 항등함수로 근사(Athalye et al. BPDA 기법). 공격은 이 BPDA 근사로 생성하되, 최종 robust accuracy는 각 방어의 **실제** 평가 파이프라인(RSR/제안 방식은 R=10 다수결)으로 측정해 방법론적 정합성 확보.
  - 시간 절약을 위해 테스트셋 전체(1만장) 대신 무작위 1000장(seed=42)으로 평가. 1000장 clean accuracy(93.20%/91.60%/89.00%)가 기존 전체 테스트셋 결과(93.80%/92.55%/89.21%)와 거의 일치해 샘플링 신뢰성 확인.
  - **결과 (eps=8/255)**:
    | 방식 | Clean | FGSM | PGD (T=20) |
    |---|---|---|---|
    | 방어 없음 | 93.20% | 12.70% | 0.00% |
    | 원본 RSR | 91.60% | 20.70% | 0.00% |
    | 제안 방식 (K=5, floor=0.36) | 89.00% | 18.10% | 0.20% |
  - **해석**: FGSM(단일 스텝)에서는 RSR·제안 방식이 방어없음보다 뚜렷이 높은 견고성을 보이지만(20.70%/18.10% vs 12.70%), BPDA를 적용한 PGD(20스텝, 적응형 공격) 앞에서는 세 방식 모두 사실상 0%대로 완전히 붕괴함. 이는 "obfuscated gradients"(Athalye et al.) 연구에서 지적된 전형적 패턴과 일치 — 확률적 전처리/재구성 기반 방어는 약한 공격에는 견고해 보이지만 적응형 강공격 앞에서는 실질적 방어력을 거의 제공하지 못함. 즉 clean accuracy 손실(RSR -1.6%p, 제안 방식 -4.2%p, 1000장 기준)이 방어력 향상으로 이어지지 않았음을 시사.
  - 다음 단계: ε ∈ {4/255, 16/255}로 PGD 스윕 예정 (현재는 8/255만 평가).
- **결정론적 대조군 구현 및 검증 (가설: 무작위성 제거 시 clean↑, robust↓)**: `models/deterministic.py`, `attacks/evaluate_deterministic.py` 추가.
  - K=5의 모든 그룹 포함/제외 조합(2^5=32가지)을 열거하고, 각 조합의 확률 π = Π p_b'(포함)×(1-p_b')(제외)을 계산해 소프트맥스 출력을 π로 가중 평균 → 제안 방식(R=10 몬테카를로 샘플링)이 근사하는 기댓값을 정확히 계산.
  - 조합 마스크가 이미지값과 무관한 고정 0/1 텐서라 SVD 재구성부터 가중평균까지 전 과정이 미분 가능 → **BPDA 불필요, 공격자가 정확한 기울기로 직접 공격 가능** (attacks.py에 `loss_fn` 파라미터 추가해 로그-확률 출력에 nll_loss 사용하도록 일반화).
  - 같은 1000장·eps=8/255로 제안 방식(무작위, BPDA)과 나란히 비교:
    | 방식 | Clean | FGSM | PGD (T=20) |
    |---|---|---|---|
    | 제안 방식 (무작위 R=10, BPDA 공격) | 89.00% | 17.10% | 0.30% |
    | 결정론적 (정확한 기댓값, 정확한 기울기) | 87.90% | 18.70% | 0.20% |
  - **가설 미확인**: clean accuracy는 오히려 소폭 하락(-1.1%p), FGSM은 오히려 소폭 상승(+1.6%p, 가설과 반대 방향), PGD는 방향은 가설과 일치하나 둘 다 이미 0%대라 유의미하지 않음.
  - **원인 추정**: (1) PGD는 바닥 효과 — 제안 방식이 이미 0.30%까지 붕괴해 결정론화로 더 떨어질 여지가 거의 없음(가설을 제대로 검증하려면 더 약한 공격에서 비교 필요). (2) 집계 방식 차이가 혼입 변수 — 제안 방식은 "R=10개 argmax 하드 결정의 다수결"인 반면 결정론적 방식은 "32개 브랜치 소프트맥스 확률의 가중평균 후 argmax"로, 애초에 다른 집계 규칙이라 순수하게 "무작위성 유무" 효과만 분리하기 어려움. (3) 이 방어 조합 자체가 원래 무작위성으로부터 얻는 실질적 이득(gradient obfuscation)이 크지 않았을 가능성.
  - **후속 실험: 더 약한 공격 ε=4/255로 재검증 (바닥 효과 회피 목적)**: 같은 1000장, PGD는 T=20/α=2/255 그대로 유지하고 ε만 8/255→4/255로 낮춰 재실행.
    | ε | 방식 | Clean | FGSM | PGD (T=20) |
    |---|---|---|---|---|
    | 4/255 | 제안 방식 (무작위 R=10, BPDA) | 89.00% | 32.00% | **5.80%** |
    | 4/255 | 결정론적 (정확한 기댓값) | 87.90% | 26.60% | **0.90%** |
  - **가설 확인(robust accuracy 부분)**: ε=8/255에서 관찰되지 않던 격차가 ε=4/255에서는 뚜렷하게 드러남 — PGD 5.80%→0.90%(약 1/6로 감소), FGSM도 32.00%→26.60%로 감소(방향이 가설과 일치하는 쪽으로 뒤집힘). 즉 ε=8/255에서 차이가 안 보였던 건 "무작위성이 실제로 도움 안 됨"이 아니라 "두 방식 다 이미 완전히 뚫려서 차이를 관찰 못 한 바닥 효과"였음을 확인.
  - **가설 미확인(clean accuracy 부분)은 유지**: 이번에도 결정론적 방식의 clean accuracy(87.90%)가 무작위 방식(89.00%)보다 낮게 나와, "정확한 기댓값이 clean accuracy를 높여준다"는 가설 쪽은 두 번의 실험(ε=8/255, 4/255) 모두 일관되게 반증됨.
- **EOT(Expectation over Transformation) 공격 구현 및 최적화**: `attacks/attacks.py`에 `eot_grad`/`eot_fgsm_attack`/`eot_pgd_attack` 추가, `attacks/evaluate_eot.py` 추가.
  - 기존 BPDA는 매 스텝마다 확률적 재구성 1회 샘플로만 기울기를 계산하는 근사였음. EOT는 `eot_samples`(기본 10)개의 독립적인 확률적 재구성을 평균낸 기울기를 사용 — 무작위 방어를 공격하는 방법론적으로 더 올바른 방식(Athalye et al.). 순차 루프 대신 `eot_samples`개를 하나의 배치로 타일링(tiling)해 단일 forward+backward로 처리하도록 벡터화해서 성능 확보(autograd의 expand 역전파가 자동으로 사본들의 기울기를 원본 x로 합산해줌).
  - **메모리 최적화 점검**: DataLoader는 이미 전부 num_workers=0~2였고, 정확도/다수결 계산 함수는 이미 전부 `@torch.no_grad()`로 감싸져 있어서 추가 수정 불필요했음. 실제로 빠져있던 부분(배치/스텝마다 명시적 `del` + `torch.cuda.empty_cache()`)만 `pgd_attack`/`eot_pgd_attack`/`eot_grad`/`craft_adversarial`에 추가.
  - 결과: GPU 메모리 피크 833.2MB(4096MB 중, 이전 결정론적 32-조합 실행의 3943MB 대비 훨씬 여유), 1000장 전체(PGD-EOT 40배치×20스텝 포함) 소요 시간 약 2~3분으로 매우 빠름.
  - **결과 (1000장, ε=8/255)**:
    | 방식 | Clean | FGSM | PGD (T=20) |
    |---|---|---|---|
    | 제안 방식 — 단일 샘플 BPDA (기존) | 89.00% | 17.10% | 0.30% |
    | 제안 방식 — EOT (N=10) | 89.00% | **14.00%** | **0.10%** |
    | 결정론적 (정확한 기댓값) | 87.90% | 18.70% | 0.20% |
  - **핵심 발견**: (1) EOT가 단일 샘플 BPDA보다 확실히 더 강한 공격 — FGSM 17.10%→14.00%, PGD 0.30%→0.10%로 둘 다 하락, 기존 BPDA가 이 방어의 취약성을 과소평가하고 있었음을 확인. (2) **결론이 역전됨**: 단일 샘플 BPDA로는 제안 방식(무작위)이 결정론적 방식보다 약간 더 견고해 보였는데(FGSM 17.10% vs 18.70%), 제대로 된 EOT로 공격하면 오히려 무작위 방식이 더 취약해짐(FGSM 14.00% < 18.70%; PGD도 0.10% < 0.20%, 다만 둘 다 0%대라 통계적으로는 미미). 즉 이 무작위 재구성 방어가 보여주던 "추가 견고성"은 상당 부분 부적절한(약한) 공격 평가 때문이었고, 방법론적으로 올바른 적응형 공격(EOT) 앞에서는 무작위성이 실질적 이득을 주지 못하거나 오히려 손해라는 것이 확인됨 — obfuscated gradients 가설을 한층 더 강하게 뒷받침.
  - **⚠️ 후속 검증(ε=4/255)에서 위 (2)번 결론이 바닥 효과 아티팩트였음이 드러남 — 아래 두 항목 참고. ε=8/255 단독 결과만으로 일반화하면 안 됨.**
- **EOT를 ε=4/255(바닥 효과 없는 구간)로 재검증 — 결론이 지표별로 갈림**: 같은 1000장(seed=42), PGD T=20/α=2/255/eot_samples=10 그대로 유지, ε만 8/255→4/255.
  - **결과 (1000장, ε=4/255)**:

    | 방식 | FGSM | PGD (T=20) |
    |---|---|---|
    | 제안 방식 — 단일 샘플 BPDA (기존) | 32.00% | 5.80% |
    | 제안 방식 — EOT (N=10) | **25.40%** | **3.10%** |
    | 결정론적 (정확한 기댓값) | 26.60% | 0.90% |

  - **질문1: EOT가 단일 BPDA보다 더 강한 공격인가? → 재현됨.** FGSM 32.00%→25.40%, PGD 5.80%→3.10%로 둘 다 하락. 독립 이표본비율 z-검정(근사) 기준 FGSM |z|≈3.27, PGD |z|≈2.93로 둘 다 통계적으로 유의 — ε=8/255 때와 동일한 패턴이 재현되어, EOT가 진짜로 더 강한 공격임을 재확인.
  - **질문2: "제안 방식(EOT) < 결정론적" 역전이 유지되는가? → 지표별로 결론이 갈림.**
    - FGSM: 25.40% < 26.60%로 방향은 ε=8/255와 같지만, z-검정 |z|≈0.61로 통계적으로 유의하지 않음 — 사실상 차이 없음으로 봐야 함. (ε=8/255의 FGSM 차이 14.00% vs 18.70%는 |z|≈2.85로 유의했었음 — ε에 따라 유의성 여부가 달라짐.)
    - PGD: 3.10% > 0.90%로 오히려 반대 방향 — 무작위 방식(EOT)이 결정론적 방식보다 확실히 더 견고함. z-검정 |z|≈3.5로 통계적으로 유의(바닥 효과가 없는 구간이라 우연으로 보기 어려움).
  - **결론 수정**: ε=8/255에서 봤던 "EOT 앞에서 무작위 방식이 결정론적 방식보다 더 취약하다"는 결론은 일반화되지 않음. 그 결과는 두 방식 다 0%대로 붕괴한 바닥 효과 구간에서 나온 것이었고, 바닥 효과가 없는 ε=4/255에서는 PGD 기준으로 오히려 무작위성이 유의미하게 도움이 됨. FGSM은 두 ε 모두 방향은 일관("제안 방식이 근소하게 열세")되지만 ε=4/255에서는 유의하지 않아 사실상 동등. 즉 "무작위성이 EOT 앞에서 이득이 되는지"는 공격 강도(ε)와 공격 종류(단일스텝 vs 반복)에 따라 결론이 달라지는 문제이며, 단일 ε의 결과만으로 일반화하면 안 된다는 방법론적 교훈을 얻음.
- **위 z-검정을 McNemar 검정(쌍체 데이터)으로 재검증**: `attacks/mcnemar_eot.py` 추가. 위에서 쓴 이표본비율 z-검정은 두 그룹이 독립표본이라고 가정하지만, 실제로는 같은 1000장 이미지를 방식만 바꿔 비교한 **쌍체(paired) 데이터**라 McNemar 검정이 통계적으로 더 적절함 — 두 방식이 서로 다르게 판단한(한쪽만 맞춘) 이미지 수만으로 검정.
  - 같은 1000장·ε=4/255 조건에서 단일 BPDA/EOT/결정론적 6가지(각 FGSM·PGD)를 다시 크래프팅하고 이미지별 정오답을 저장해 재계산. (재계산 과정에서 실행별 확률적 샘플링 차이로 EOT 수치가 FGSM 26.00%/PGD 3.00%로 소폭 재현됨 — 원래 값(25.40%/3.10%)과 0.1~0.6%p 차이는 정상적인 몬테카를로 변동.)
  - **결과**:

    | 비교 | 지표 | 불일치 쌍 (A만 정답 / B만 정답) | McNemar 통계량 | p-value | 유의성 |
    |---|---|---|---|---|---|
    | Q1a: EOT(A) vs 단일 BPDA(B) | FGSM | 40 / 100 | χ²=24.864 | 6.15e-07 | **유의함** |
    | Q1b: EOT(A) vs 단일 BPDA(B) | PGD | 0 / 28 | χ²=26.036 | 3.35e-07 | **유의함** |
    | Q2a: 제안-EOT(A) vs 결정론적(B) | FGSM | 66 / 72 | χ²=0.181 | 0.6704 | 유의하지 않음 |
    | Q2b: 제안-EOT(A) vs 결정론적(B) | PGD | 23 / 2 | χ²=16.000 | 6.33e-05 | **유의함** |

  - **z-검정과의 방향/유의성 일치 여부**: 4개 비교 전부 **방향과 유의성 판정이 z-검정과 일치**함 — (1) EOT가 단일 BPDA보다 강한 공격이라는 결론(Q1a, Q1b)은 z-검정보다도 더 강하게(p값이 훨씬 작게) 확인됨. (2) FGSM에서 제안-EOT와 결정론적의 차이는 두 검정 모두 유의하지 않음(Q2a) — "사실상 동등"이라는 결론 재확인. (3) PGD에서 무작위 방식(제안-EOT)이 결정론적보다 더 견고하다는 결론(Q2b)도 두 검정 모두 유의함 — 쌍체 데이터를 올바르게 반영한 McNemar 검정으로도 뒤집히지 않아, 이 결론이 통계적으로 견고함을 확인.

## 2026-09-07

### 10단계: 하이퍼파라미터 스윕

**개요**: `attacks/sweep.py` 추가. A/B/C/D 4개 구간을 자동 순회하며 제안 방식(EOT)과 결정론적 대조군을 다양한 조건에서 비교. 매 (설정, 방식, 공격) 조합마다 이미지별 정오답(0/1)을 `results/sweep/<섹션>/*.npy`로 저장(추후 McNemar 등 재계산이 재실행 없이 가능하도록), 집계 정확도는 `results/sweep/summary.json`에 누적 기록. 모든 실험은 동일한 1000장(seed=42) 기준.

**실행 중 이슈와 대응**: 전체 소요 시간이 최초 추정(1.5~2시간)보다 훨씬 길었음(Section A만 7개 floor × 약 23분 ≈ 2.7시간). 두 차례 시스템 메모리 부족으로 프로세스가 강제 종료됨(1차: B/K8 결정론적 PGD 도중 전체 스윕 사망, 2차: 재시도한 B 섹션도 같은 지점에서 재차 사망). 대응: (1) `summary.json` 기반 재개(resume) 기능 추가 — 이미 완료된 (섹션,설정,방식,공격) 조합은 자동으로 건너뜀. (2) 결정론적 방식의 실효 배치 크기를 1024→256으로 낮추고 `gc.collect()`를 추가. (3) 긴 단일 프로세스 대신 섹션(B/C/D) 단위로 프로세스를 재시작하며 진행. 이후 재발 없이 완주함 — 매 설정 완료 즉시 저장되는 구조 덕분에 두 차례 종료에도 데이터 손실은 없었음.

**A) floor 스윕 (K=5, eps=4/255, 메인)**

| floor | 제안(EOT) Clean | 제안 FGSM | 제안 PGD | 결정론적 Clean | 결정론적 FGSM | 결정론적 PGD |
|---|---|---|---|---|---|---|
| 0.05 | 80.70% | 19.00% | 2.40% | 80.90% | 17.10% | 0.40% |
| 0.10 | 81.90% | 20.70% | 2.20% | 81.30% | 18.70% | 0.40% |
| 0.15 | 83.30% | 22.00% | 2.30% | 81.60% | 19.80% | 0.50% |
| 0.20 | 85.40% | 23.00% | 2.50% | 82.30% | 21.20% | 0.80% |
| 0.25 | 86.40% | 23.60% | 2.90% | 84.00% | 23.20% | 0.80% |
| 0.30 | 88.00% | 25.50% | 2.90% | 86.40% | 24.60% | 1.10% |
| 0.36 | 89.00% | 26.00% | 3.00% | 87.90% | 26.60% | 0.90% |

**추이**: floor를 높일수록(평균적으로 더 많은 성분이 항상 포함되는 방향) 두 방식 모두 clean·FGSM 정확도가 단조 증가함 — floor는 사실상 "평균적으로 보존하는 정보량"의 손잡이이므로 당연한 방향. 흥미로운 건 PGD: 제안 방식(무작위)은 floor 전 구간에서 2.2~3.0%로 완만하게만 오르는 반면 결정론적 방식은 0.4~1.1%로 계속 훨씬 낮게 깔려, **"무작위 방식이 PGD에 더 견고하다"는 격차가 floor 값과 무관하게 전 구간에서 거의 일정하게 유지**됨.

**통계적 유의성 (7개 floor 전체, 독립 이표본비율 z-검정)**: PGD는 **7개 floor 전부**에서 유의함(|z|≈2.9~3.8, 전부 p<0.01) — 무작위 방식이 결정론적 방식보다 유의하게 견고하다는 결론이 floor 전 구간에서 재현됨. FGSM은 반대로 **7개 floor 전부**에서 유의하지 않음(|z|≈0.5~1.2).

**대표 지점 McNemar 검정(쌍체, 저장된 .npy 재사용, 재실행 없음)**: floor=0.05(신규)와 floor=0.36(기존 결과 재확인)에서 계산.

| floor | 지표 | 불일치 쌍(제안만정답/결정론만정답) | 통계량 | p-value | 유의성 |
|---|---|---|---|---|---|
| 0.05 | FGSM | 41 / 22 | χ²=5.143 | 0.0233 | **유의함** |
| 0.05 | PGD | 22 / 2 | 이항검정 | 3.59e-05 | **유의함** |
| 0.36 | FGSM | 66 / 72 | χ²=0.181 | 0.6704 | 유의하지 않음 |
| 0.36 | PGD | 23 / 2 | χ²=16.000 | 6.33e-05 | **유의함** |

**흥미로운 발견**: floor=0.05에서는 McNemar(쌍체)가 FGSM도 유의하다고 판정(p=0.023)하는데, 이는 독립표본 z-검정(|z|≈1.10, 유의하지 않음)과 결론이 갈림 — 같은 1000장을 비교하는 이 실험 설계에서는 상관관계를 무시하는 z-검정보다 McNemar가 검정력이 높기 때문으로 보이며, 앞서 세운 "쌍체 데이터엔 McNemar가 더 적절하다"는 방법론적 결론을 다시 뒷받침함. PGD는 두 floor 모두, 두 검정 모두 일관되게 유의함.

**B) K 스윕 (eps=4/255, keep_ratio≈0.5로 floor를 해석적으로 맞춤)**

| K | floor(해석적) | keep_ratio | 제안 Clean | 제안 FGSM | 제안 PGD | 결정론적 Clean | 결정론적 FGSM | 결정론적 PGD |
|---|---|---|---|---|---|---|---|---|
| 3 | 0.2390 | ≈0.500 | 90.60% | 25.00% | 1.40% | 89.80% | 24.20% | 0.50% |
| 5 (A 재사용) | 0.36 | ≈0.4995 | 89.00% | 26.00% | 3.00% | 87.90% | 26.60% | 0.90% |
| 8 | 0.4309 | ≈0.500 | 83.00% | 34.30% | 6.30% | 85.70% | 33.20% | 1.40% |
| 16 | 0.4694 | ≈0.500 | 74.30% | 35.10% | 6.90% | N/A (2^16=65536 조합 계산 불가로 생략, 사용자 확인 하에 결정) | N/A | N/A |

**추이**: K를 키울수록(그룹을 잘게 쪼갤수록) keep_ratio를 0.5로 고정했음에도 **clean accuracy는 단조 감소**(90.60→89.00→83.00→74.30, 제안 방식 기준)하는 반면 **FGSM·PGD robust accuracy는 오히려 단조 증가**(FGSM 25.00→26.00→34.30→35.10%, PGD 1.40→3.00→6.30→6.90%)함. 즉 그룹을 세분화할수록 "평균 정보량(keep_ratio)은 같아도" 클린 정확도를 내주고 강건성을 얻는 트레이드오프가 뚜렷하게 나타남 — 이전(K=5 vs K=8) 단일 비교에서는 "clean accuracy만 나빠진다"고만 봤는데, 공격까지 넣어 보니 그 대신 robust accuracy는 오히려 개선되는 구조였음이 이번 스윕에서 새로 드러남.

**C) eot_samples 스윕 (floor=0.05, K=5, eps=4/255, 제안 방식만)**

| eot_samples | FGSM | PGD |
|---|---|---|
| 5 | 19.20% | 2.60% |
| 10 (A 재사용) | 19.00% | 2.40% |
| 20 | 19.60% | 2.40% |
| 40 | 20.90% | 2.30% |

**추이**: eot_samples를 5→40으로 8배 늘려도 FGSM(19.0~20.9%)·PGD(2.3~2.6%) 모두 뚜렷한 단조 추세 없이 오차 범위 내에서만 등락함 — N=10이 이미 충분히 수렴된 EOT 추정치였음을 시사. 공격을 더 정밀하게 만들고 싶다면 eot_samples를 늘리는 것보다 다른 지렛대(eps, PGD 스텝 수 등)가 더 효과적일 것으로 보임.

**D) eps 교차검증 (floor=0.05, K=5, N=10)**

| eps | 제안 Clean | 제안 FGSM | 제안 PGD | 결정론적 Clean | 결정론적 FGSM | 결정론적 PGD |
|---|---|---|---|---|---|---|
| 4/255 (A 재사용) | 80.70% | 19.00% | 2.40% | 80.90% | 17.10% | 0.40% |
| 8/255 (신규) | 80.70% | 11.00% | 0.10% | 80.90% | 12.90% | 0.20% |

(참고 - floor=0.36/N=10 기존 결과: eps=4/255 → 제안 89.00/26.00/3.00, 결정론적 87.90/26.60/0.90; eps=8/255 → 제안 89.00/14.00/0.10, 결정론적 87.90/18.70/0.20)

**추이**: floor=0.05에서도 floor=0.36 때와 동일한 패턴이 재현됨 — eps를 8/255로 올리면 두 방식 모두 PGD가 0%대로 붕괴하고, 이 지점에서 결정론적 방식(0.20%)이 제안 방식(0.10%)보다 근소하게 높아지는 **순서 역전**이 다시 나타남(floor=0.36에서도 동일 패턴: 0.20% vs 0.10%). 즉 이전에 "바닥 효과"로 설명했던 순서 역전 현상이 **floor 값과 무관하게 eps=8/255에서 일관되게 재현**됨 — 바닥 효과 가설이 floor=0.05라는 독립적인 조건에서도 다시 확인됨.

### 적응형 그룹 확률 (문제1 해결 시도)

**문제 제기**: 제안 방식의 그룹별 포함 확률 p_b'가 `calibrate_group_probabilities()`로 학습셋 500장 평균에서 **한 번만** 계산된 뒤, 모든 테스트 이미지에 동일한 고정 벡터로 재사용되고 있음을 발견. 이는 원본 RSR이 갖던 "이미지마다 새로 SVD해서 그 이미지 고유의 특이값 분포에 비례해 확률을 계산한다"는 적응형 성질을 제안 방식에서는 잃어버린 것 — 학습셋 평균이라는 population-level 정보로 대체된 셈. 이를 고쳐 이미지별로 즉석에서 p_b'를 계산하는 **적응형(adaptive)** 버전을 구현하고 기존 고정형(fixed)과 비교했다.

**구현**: `models/proposed.py`에 `compute_adaptive_group_probs(S, buckets, floor)`(그 배치 이미지 자신의 SVD 특이값으로 그룹별 에너지 비중 → floor 적용)와 `sample_group_mask_from_probs()`(이미지별로 다른 (n,c,K) 확률 텐서에 대한 베르누이 샘플링 + 전부 제외시 최고 확률 그룹 강제 포함), `evaluate_proposed_adaptive()`를 추가. 기존 고정형 코드(`calibrate_group_probabilities`, `evaluate_proposed`)는 그대로 유지하고, `proposed.py`에 `--adaptive-probs` 플래그로 전환 가능하게 함. 그룹 분할(buckets, K개 성분개수 균등분할) 자체는 고정형과 동일 — 오직 "포함 확률을 어디서 가져오는가"만 다름.

**정합성 체크** (`attacks/check_adaptive_sanity.py`, 5장, K=5, floor=0.36): 이미지별 p_b(floor 적용 전)가 실제로 서로 다름을 확인 — 최상위 그룹(B0) 에너지 비중이 이미지마다 98.93%~99.80% 사이에서 변동(5장 모두 동일하지 않음, `all images identical` 검사 결과 False). **다만 흥미로운 부작용 발견**: B1~B4는 원래 에너지 비중이 전부 0.02~0.89% 수준으로 극히 작아, floor=0.36을 적용하면 5장 전부 예외 없이 정확히 0.36으로 클램프됨 — 즉 이번 floor 설정에서는 "적응형"이라 해도 실질적으로 차이가 나는 건 B0 하나뿐이고 그 차이조차 1%p 미만으로 작음. 참고용으로 계산한 고정형 calibration 값(p_b_prime = [0.9943, 0.36, 0.36, 0.36, 0.36])과 비교해도 사실상 거의 같은 벡터가 나옴.

**Clean/FGSM/PGD-EOT 비교** (1000장, seed=42, K=5, floor=0.36, eps=4/255, PGD T=20/α=2/255/eot_samples=10):

| 방식 | Clean | FGSM | PGD-EOT |
|---|---|---|---|
| 고정형 (기존, 학습셋 500장 평균 p_b' 고정) | 89.10% | 31.80% | 2.80% |
| 적응형 (신규, 이미지별 즉석 p_b') | 89.20% | 31.90% | 2.50% |
| 참고: 원본 RSR | 92.55%(1만장 기준) | - | - |

(참고: floor=0.36/N=10 기존 결과와 비교 시 clean 89.00%·FGSM 26.00%·PGD 3.00%였는데, 이번 실행은 별도 seed 스트림(fixed용/adaptive용 generator를 분리)이라 소폭 차이 — 몬테카를로 변동 범위 내.)

**McNemar 검정 (적응형 vs 고정형, 쌍체, 같은 1000장)**:

| 지표 | 불일치 쌍(적응형만정답/고정형만정답) | p-value | 유의성 |
|---|---|---|---|
| Clean | 1 / 0 | 1.0 | 유의하지 않음 |
| FGSM | 2 / 1 | 1.0 | 유의하지 않음 |
| PGD-EOT | 0 / 3 | 0.25 | 유의하지 않음 |

**해석**: Clean accuracy(+0.10%p), FGSM(+0.10%p)은 미세하게 개선, PGD-EOT(-0.30%p)는 오히려 미세하게 하락 — 방향조차 지표마다 다르고, 세 지표 모두 불일치 쌍이 0~3개뿐이라 McNemar 검정상 통계적으로 유의한 차이가 전혀 없음. 즉 **"이미지별 적응형으로 고치면 원본 RSR과의 격차가 줄어들 것"이라는 기대는 이번 조건(floor=0.36)에서는 확인되지 않음** — 개선도 악화도 아닌 사실상 동일한 성능.

**원인 추정**: 정합성 체크에서 이미 드러났듯, floor=0.36이라는 값 자체가 이미지 간 실제 에너지 분포 차이를 거의 다 지워버림 — B1~B4는 원본 에너지 비중이 모든 이미지에서 1% 미만이라 항상 floor(36%)에 클램프되고, 유일하게 살아있는 B0의 이미지 간 변동폭도 1%p 미만이라 R=10 베르누이 시행의 결과(어느 그룹이 켜지는지)에 사실상 영향을 주지 못함. 다시 말해 이번 비교는 "적응형 자체가 무의미하다"를 보여준 게 아니라, **floor를 공정 비교를 위해 고정형과 똑같이 0.36으로 맞춘 선택이 적응형의 효과를 구조적으로 억눌렀을 가능성**이 크다. floor=0.05처럼 낮은 값에서는 B1~B4의 실제(작지만 0은 아닌) 에너지 비중이 그대로 확률에 반영되어 이미지별 차이가 더 크게 드러날 것으로 예상되며, 이는 아직 검증되지 않은 후속 실험 후보임.

#### Floor 스윕으로 위 가설 검증 (floor ∈ {0.05, 0.10, 0.15, 0.20, 0.36})

**가설**: floor가 낮아질수록 B1~B4가 덜 클램프되어(원본 에너지 비중이 floor보다 커지는 이미지가 늘어) 이미지별 차이가 확률에 더 반영되고, 그 결과 고정형·적응형 사이 격차도 커질 것이다.

`attacks/evaluate_adaptive_sweep.py` 추가(같은 1000장·seed=42·K=5·eps=4/255·PGD T=20/α=2/255/eot_samples=10 유지, floor=0.36은 이전 실행 결과를 재사용하고 정합성 체크만 재계산, 나머지 4개 floor는 전체 파이프라인 신규 실행, floor마다 즉시 `results/adaptive_sweep/summary.json`에 저장해 재개 가능하게 구성).

**정합성 체크 결과 — 가설의 전제 자체가 이 floor 범위에서 성립하지 않음**: 1000장 전체에서 측정한 채널평균 raw p_b(floor 적용 전) 범위는 B0=96.21~99.99%(평균 99.40%), **B1=0.003~3.29%**, B2=0.00002~0.58%, B3≈0~0.087%, B4≈0~0.0076%다. B1의 최댓값(3.29%)조차 스윕에서 가장 낮게 시도한 floor(0.05=5%)보다 작다 — 즉 **B1~B4는 0.05~0.36 사이의 5개 floor 전부에서, 1000장 전원이 예외 없이 100% 클램프됨**(클램프 안 되는 이미지 비율 = 0%, 모든 floor에서 동일). floor를 0.36→0.05로 낮춰도 "그 floor보다 자연 에너지 비중이 큰 이미지가 나타나 클램프가 풀리는" 일이 이 범위에서는 전혀 일어나지 않는다.

**Clean/FGSM/PGD-EOT 비교 (floor별 전체 표)**:

| floor | 고정형 Clean | 고정형 FGSM | 고정형 PGD | 적응형 Clean | 적응형 FGSM | 적응형 PGD | McNemar p (Clean/FGSM/PGD) |
|---|---|---|---|---|---|---|---|
| 0.05 | 80.80% | 21.50% | 2.40% | 80.70% | 21.50% | 2.40% | 1.000 / 1.000 / 1.000 |
| 0.10 | 81.40% | 23.20% | 2.70% | 81.40% | 23.20% | 2.50% | 1.000 / 1.000 / 0.500 |
| 0.15 | 82.90% | 24.80% | 2.90% | 82.90% | 24.90% | 2.90% | 1.000 / 1.000 / 1.000 |
| 0.20 | 84.40% | 27.80% | 2.60% | 84.40% | 28.00% | 2.50% | 1.000 / 0.625 / 1.000 |
| 0.36 | 89.10% | 31.80% | 2.80% | 89.20% | 31.90% | 2.50% | 1.000 / 1.000 / 0.250 |

**가설 기각**: 고정형-적응형 격차(적응형-고정형)는 Clean -0.1~+0.1%p, FGSM 0.0~+0.2%p, PGD -0.3~0.0%p 범위에서 5개 floor 전체에 걸쳐 등락하며, floor를 낮출수록 격차가 커지는 단조 추세는 **관찰되지 않음**. McNemar 검정도 5개 floor·3개 지표(총 15개 비교) 전부 p≥0.25로 통계적으로 유의한 차이가 단 한 곳도 없다 — "어느 floor에서 처음 유의해지는가"라는 질문 자체가 이 범위에서는 답이 "없음"이다.

**해석**: 가설이 틀린 게 아니라 가설이 전제한 메커니즘(floor를 낮추면 B1~B4의 클램프가 풀린다)이 애초에 이 floor 범위(0.05~0.36)에서 작동하지 않았다는 게 원인 — B1의 최댓값(3.29%)조차 스윕에서 가장 낮은 floor(5%)에 못 미치므로, B1~B4는 사실상 상수(=floor)로 남아 있고 스윕 내내 유일하게 살아있는 이미지별 신호는 B0(96~100% 사이, 변동폭 1000장 기준 약 3.8%p)뿐이었다. 즉 이 실험은 "이미지별 적응이 원본 RSR과의 격차를 못 줄인다"를 반박하지 못했을 뿐 아니라, 그 이유가 "적응형 신호가 약해서"가 아니라 "이 K=5 그룹 분할에서 최상위 그룹 하나가 에너지의 99%+를 독식하는 구조상, floor를 이 범위 안에서 조정하는 것만으로는 B1~B4가 이미지별로 다르게 반응할 여지 자체가 생기지 않는다"는 구조적 이유임을 확인시켜준다. B1~B4의 클램프를 실제로 풀어보려면 floor를 3% 미만(예: 0.01~0.03)까지 낮추거나, K를 줄여 상위 그룹에 더 많은 성분을 몰아주는 등 그룹 분할 자체를 바꿔야 할 것으로 보이며, 이는 검증되지 않은 후속 실험 후보다.

#### 클램프가 실제로 풀리는 초저 floor 재검증 (floor ∈ {0.005, 0.01, 0.02})

앞선 결론("B1의 최댓값(3.29%)이 스윕 최저 floor(5%)보다 작아 클램프가 전혀 안 풀림")을 근거로, floor를 3.29%보다 확실히 낮은 지점까지 내려 클램프가 실제로 풀리는 구간에서도 결론이 유지되는지 재검증했다.

**1단계 (저비용 사전 확인, `attacks/check_clamp_release.py`)**: 전체 파이프라인 재실행 없이, 이미 계산된 1000장의 raw p_b(채널평균, floor 적용 전)만 재사용해 floor 후보별로 "B1~B4 중 하나라도 클램프가 안 풀리는(raw > floor) 이미지 비율"을 집계:

| floor | 하나 이상 언클램프 비율 | B1 언클램프 | B2 언클램프 | B3/B4 언클램프 |
|---|---|---|---|---|
| 0.005 (0.5%) | **40.70%** | 40.70% | 0.30% | 0.00% |
| 0.01 (1%) | **11.40%** | 11.40% | 0.00% | 0.00% |
| 0.02 (2%) | 1.60% | 1.60% | 0.00% | 0.00% |
| 0.03 (3%) | 0.30% | 0.30% | 0.00% | 0.00% |

"10% 이상"을 기준으로 0.005·0.01·0.02 세 지점을 선정(0.02는 1.6%로 기준 미달이지만 40.7%→11.4%→1.6% 추이를 보기 위해 포함, 사용자 확인 하에 결정)해 2단계로 진행.

**2단계 (전체 파이프라인, 같은 1000장·K=5·eps=4/255·PGD T=20/α=2/255/eot_samples=10)**:

| floor | B1 언클램프 비율 | 고정형 Clean/FGSM/PGD | 적응형 Clean/FGSM/PGD | McNemar p (Clean/FGSM/PGD) |
|---|---|---|---|---|
| 0.005 | 40.70% | 80.70/18.90/2.40 | 80.70/19.10/2.40 | 1.000/0.500/1.000 |
| 0.01 | 11.40% | 80.70/19.50/2.50 | 80.70/19.50/2.60 | 1.000/1.000/1.000 |
| 0.02 | 1.60% | 80.70/19.90/2.30 | 80.70/19.90/2.30 | 1.000/1.000/1.000 |

(예상대로 floor가 0.005~0.02로 극히 낮아지면서 clean accuracy가 80.7% 선으로 떨어짐 — floor=0.05의 80.8%, floor=0.36의 89.1%보다 낮은 것은 keep_ratio 자체가 낮아진 정상적인 트레이드오프.)

**최종 판정: 가설 기각(클램프가 실제로 풀리는 구간에서도 유지)**. B1이 이미지의 40.7%에서 실제로 언클램프되는 floor=0.005에서조차, 고정형-적응형 격차는 Clean 0.0%p·FGSM +0.2%p·PGD 0.0%p로 여전히 무시할 수준이고, McNemar 검정도 세 지표 모두 p≥0.5로 유의하지 않다(FGSM의 불일치 쌍은 2개뿐). "몇 번째 floor부터 유의해지는가"라는 질문의 답은 이번에도 **없음** — 0.005~0.36 총 8개 floor·3개 지표(24개 비교) 전부 유의한 차이가 발견되지 않았다.

**원인 재해석**: 클램프가 풀리는 것과 그 차이가 결과에 영향을 주는 것은 별개였다. B1은 언클램프되어도 원래 에너지 비중 자체가 작다(평균 0.53%, 최댓값 3.29%) — 즉 B1의 확률이 이미지에 따라 0.5%~3.3% 사이에서 "정확하게" 반영되든, floor로 뭉뚱그려지든, 그 확률 차이가 R=10 베르누이 시행에서 B1의 6~7개 성분이 포함될지에 주는 영향 자체가 작고, 설사 포함되어도 이미 B0(에너지 99%+, 포함 확률 99.4%)가 재구성을 압도하고 있어 분류 결과를 바꿀 만한 정보량을 추가하지 못한다. 결론적으로 **이 K=5 그룹 분할 구조(최상위 그룹이 에너지를 독식)에서는 "그룹 확률을 이미지별로 적응시키는가"라는 축 자체가, floor를 어떻게 조정하든 clean/robust accuracy에 실질적 영향을 주지 못한다** — 이는 floor 조정이 아니라 그룹 분할 방식(K를 줄이거나 상위 그룹을 더 세분화하는 등) 자체를 바꿔야 검증 가능한, 별개의 후속 질문으로 남는다.
