# SVD 기반 양자화 RSR 방어 실험 보고서

**작성일**: 2026-09-07
**데이터셋**: CIFAR-10 · **분류기**: VGG16 (5-stage conv+BN+ReLU+maxpool, 3-layer FC)
**코드**: `models/`, `attacks/` (이 저장소)

---

## 1. 배경 및 동기

RSR(Random Self-Reconstruction, 채널별 SVD 성분을 특이값 크기에 비례한 확률로 비복원추출해 재구성한 뒤 R회 다수결)은 적대적 공격에 대한 확률적 전처리 방어 계열의 하나다. 이 프로젝트는 RSR을 다음과 같이 변형한 **제안 방식**을 설계하고, 오리지널 RSR·완전 결정론적 대조군과 함께 clean accuracy와 적대적 강건성(FGSM/PGD, BPDA/EOT 적응형 공격)을 비교 평가했다.

**제안 방식의 핵심 아이디어**: 개별 성분 단위 확률적 선택 대신, 특이값을 개수 기준으로 K개 그룹으로 균등 분할하고, 그룹별로 학습셋에서 추정한 평균 에너지 비중(floor로 하한 설정)을 포함 확률로 삼아 그룹 단위 베르누이 시행으로 성분을 켜고 끈다. 목적은 개별 성분 단위보다 성긴(coarse-grained) 무작위성이 강건성에 어떤 영향을 주는지, 그리고 그 무작위성 자체가 실제로 유의미한 방어 효과를 갖는지를 정량 분리하는 것이었다.

## 2. 방법론

### 2.1 방어 기법 정의

| 방식 | 구현 파일 | 설명 |
|---|---|---|
| 베이스라인 | `models/vgg16_cifar10_baseline.py` | 방어 없음, 100 에폭 SGD 학습, clean accuracy 93.80% |
| 원본 RSR | `models/rsr.py` | 특이값 크기 비례 확률로 성분 비복원추출 → R=10회 다수결 |
| 성분개수 균등 양자화 | `models/quantization.py` | 32개 특이값 성분을 K개 그룹으로 개수 기준 균등분할(그룹 크기 편차 ±1). 누적 에너지 기준 분할은 실패해 폐기(§3.1) |
| 제안 방식 | `models/proposed.py` | 양자화 그룹 + 그룹별 베르누이(p_b′) 포함 확률 → R=10회 다수결 |
| 결정론적 대조군 | `models/deterministic.py` | 2^K개 포함/제외 조합 전수 열거, 각 조합 확률 π로 소프트맥스 가중평균(정확한 기댓값, 무작위성 없음) |

### 2.2 공격 및 평가 방법론

| 요소 | 구현 파일 | 설명 |
|---|---|---|
| FGSM / PGD | `attacks/attacks.py` | FGSM 단일 스텝, PGD는 T=20, α=2/255, 매 스텝 L∞ 투영+클리핑 |
| BPDA | `attacks/attacks.py` (`BPDAIdentity`) | 미분 불가능한 재구성의 forward는 그대로, backward는 항등함수로 근사 |
| EOT | `attacks/attacks.py` (`eot_grad`, `eot_*_attack`) | 확률적 재구성을 eot_samples(기본 10)회 독립 샘플링해 평균 기울기 사용 — BPDA보다 방법론적으로 더 정확한 적응형 공격 |
| 평가 파이프라인 | `attacks/evaluate*.py` | 공격은 근사 기울기(BPDA/EOT)로 생성하되, robust accuracy는 각 방어의 **실제** 추론 파이프라인(R=10 다수결 또는 정확한 기댓값)으로 측정 |
| 통계 검정 | `attacks/mcnemar_eot.py` | 이표본비율 z-검정(독립 가정) + McNemar 검정(쌍체 데이터, 같은 1000장을 방식만 바꿔 비교하므로 더 적절) |

모든 공격 평가는 테스트셋 중 무작위 1000장(seed=42)으로 수행(전체 1만장과 clean accuracy가 거의 일치함을 확인해 샘플링 신뢰성 확보).

## 3. 주요 결과

### 3.1 사전 정지 작업: 양자화 방식 버그와 수정

특이값의 누적 에너지(σ²) 비율로 K개 구간을 균등 분할하는 최초 구현은, 자연 이미지 SVD 스펙트럼에서 최상위 성분 하나가 전체 에너지의 90~99%를 차지하는 특성 때문에 대부분의 버킷이 항상 비는 문제가 발생했다. 이는 코드 버그가 아니라 자연 이미지 스펙트럼의 전형적 특성으로 확인되었고, 버킷 기준을 "성분 개수 균등분할"로 변경해 해결했다(그룹은 항상 균등하게 채워지되, 에너지 집중 현상 자체는 그대로 관찰 가능).

### 3.2 Clean Accuracy 비교 (K=5, floor 스윕)

| floor | 평균 keep_ratio | 제안 방식 | vs 원본 RSR (92.55%) |
|---|---|---|---|
| 0.05 | 0.2576 | 80.86% | -11.69%p |
| 0.10 | 0.2965 | 81.92% | -10.63%p |
| 0.20 | 0.3742 | 85.05% | -7.50%p |
| 0.30 | 0.4524 | 87.90% | -4.65%p |
| 0.36 | 0.4995 | 89.21% | -3.34%p |

- 베이스라인(무방어): **93.80%**, 원본 RSR(keep_ratio=0.5): **92.55%**
- floor=0.36에서 제안 방식의 평균 keep_ratio(0.4995)가 원본 RSR(0.5)과 사실상 동일하지만, clean accuracy는 여전히 3.34%p 낮음 → **평균 정보량(성분 개수)이 같아도, 그룹 단위 거친 선택이 개별 성분 단위 정교한 선택보다 clean accuracy에서 불리**하다는 것을 시사.

### 3.3 그룹 세분화(K) 실험 — 가설 반박

"K를 늘리면(그룹을 더 세분화하면) RSR과의 격차가 줄어들 것"이라는 가설을 검증했으나, keep_ratio≈0.5로 고정한 상태에서 K=5(-3.34%p)보다 K=8(-7.85%p)이 오히려 격차가 2배 이상 벌어져 **가설이 반박**됐다. 원인은 K가 커질수록 "항상 포함되는 핵심 상위 성분" 개수가 줄어들어(7→4개), 남은 그룹들의 무작위 on/off 비중이 늘고 시행 간 일관성이 떨어지기 때문으로 추정.

### 3.4 적대적 강건성 — BPDA 단일 샘플 (초기 평가)

ε=8/255, 1000장 기준:

| 방식 | Clean | FGSM | PGD (T=20) |
|---|---|---|---|
| 방어 없음 | 93.20% | 12.70% | 0.00% |
| 원본 RSR | 91.60% | 20.70% | 0.00% |
| 제안 방식 (floor=0.36) | 89.00% | 18.10% | 0.20% |

FGSM(약한 단일 스텝 공격)에서는 두 방어 모두 방어 없음보다 뚜렷이 견고해 보이지만, PGD(20스텝 반복, 적응형)에서는 세 방식 모두 사실상 0%로 붕괴 — obfuscated gradients(Athalye et al.)의 전형적 패턴과 일치.

### 3.5 무작위성의 실질적 가치 — 결정론적 대조군과의 비교

무작위 재구성(제안 방식, R=10 몬테카를로)과 정확한 기댓값을 계산하는 결정론적 대조군을 동일 조건에서 비교했다.

**ε=8/255** (바닥 효과 존재 구간):

| 방식 | Clean | FGSM | PGD |
|---|---|---|---|
| 제안 방식 (무작위, EOT 공격) | 89.00% | 14.00% | 0.10% |
| 결정론적 (정확한 기댓값) | 87.90% | 18.70% | 0.20% |

**ε=4/255** (바닥 효과 회피 구간, 최종 신뢰 결과):

| 방식 | FGSM | PGD |
|---|---|---|
| 제안 방식 — 단일 BPDA | 32.00% | 5.80% |
| 제안 방식 — EOT (N=10) | 25.40~26.00% | 3.00~3.10% |
| 결정론적 (정확한 기댓값) | 26.60% | 0.90% |

**McNemar 검정(쌍체, ε=4/255, floor=0.36)**:

| 비교 | 지표 | 통계량 | p-value | 유의성 |
|---|---|---|---|---|
| EOT vs 단일 BPDA | FGSM | χ²=24.864 | 6.15e-07 | 유의함 |
| EOT vs 단일 BPDA | PGD | χ²=26.036 | 3.35e-07 | 유의함 |
| 제안(EOT) vs 결정론적 | FGSM | χ²=0.181 | 0.6704 | 유의하지 않음 |
| 제안(EOT) vs 결정론적 | PGD | χ²=16.000 | 6.33e-05 | 유의함 |

**핵심 결론**:
1. **EOT는 단일 샘플 BPDA보다 확실히 더 강한 공격이다** — 기존 BPDA 평가는 무작위 방어의 취약성을 과소평가하고 있었다(모든 floor·ε 조합에서 재현, 통계적으로 매우 유의).
2. **ε=8/255에서 관찰된 "무작위성이 결정론보다 취약하다"는 결론은 두 방식이 이미 0%대로 붕괴한 바닥 효과 아티팩트였다.** 바닥 효과가 없는 ε=4/255에서는 오히려 **PGD 기준으로 무작위 방식이 결정론적 방식보다 유의하게 더 견고**하다(0.90% → 3.00%, McNemar p=6.33e-05).
3. FGSM은 ε=4/255에서 두 검정(z-검정, McNemar) 모두 유의하지 않아 사실상 두 방식이 동등하다.
4. Clean accuracy에서는 무작위 방식이 결정론적 방식보다 항상 소폭 높다(ε=8/255, 4/255 두 조건 모두 일관) — "정확한 기댓값이 clean accuracy를 높여줄 것"이라는 초기 가설은 반증됨.

### 3.6 10단계 하이퍼파라미터 스윕 (2026-09-07)

`attacks/sweep.py`로 A(floor)/B(K)/C(eot_samples)/D(eps) 4개 축을 체계적으로 스윕했다. 결과는 `results/sweep/summary.json`에 원자료로, `results/sweep/<섹션>/*.npy`에 이미지별 정오답으로 저장되어 재실행 없이 재계산 가능하다.

**A) floor 스윕 (K=5, ε=4/255)** — 7개 floor(0.05~0.36) 전 구간에서:
- floor를 높일수록 clean·FGSM은 단조 증가(당연한 방향: floor는 평균 보존 정보량의 손잡이).
- **PGD는 무작위 방식이 결정론적 방식보다 7개 floor 전부에서 유의하게 더 견고**하다(z-검정 |z|≈2.9~3.8, 전부 p<0.01) — §3.5의 결론이 단일 지점이 아니라 floor 전 구간에서 재현됨.
- FGSM은 7개 floor 전부에서 유의하지 않음(사실상 동등).

**B) K 스윕 (ε=4/255, keep_ratio≈0.5 고정)**:

| K | 제안 Clean | 제안 FGSM | 제안 PGD |
|---|---|---|---|
| 3 | 90.60% | 25.00% | 1.40% |
| 5 | 89.00% | 26.00% | 3.00% |
| 8 | 83.00% | 34.30% | 6.30% |
| 16 | 74.30% | 35.10% | 6.90% |

K를 키울수록(그룹을 잘게 쪼갤수록) **clean accuracy는 단조 감소하지만 FGSM·PGD robust accuracy는 오히려 단조 증가**한다 — keep_ratio(평균 정보량)를 고정해도 그룹 세분화 자체가 뚜렷한 clean-robust 트레이드오프 손잡이로 작동함을 새로 확인. (K=16 결정론적 대조군은 2^16=65536 조합 전수 계산이 비현실적이라 생략.)

**C) eot_samples 스윕 (floor=0.05, K=5)**: 5→40으로 8배 늘려도 FGSM(19.0~20.9%)·PGD(2.3~2.6%) 모두 오차 범위 내 등락만 있음 — N=10이 이미 충분히 수렴된 추정치.

**D) eps 교차검증 (floor=0.05, K=5)**: floor=0.05에서도 floor=0.36과 동일 패턴 재현 — ε=8/255에서 두 방식 모두 PGD가 0%대로 붕괴하며 결정론적(0.20%)이 제안(0.10%)보다 근소 우위인 순서 역전이 다시 나타남. 바닥 효과 가설이 floor와 무관하게 재확인됨.

## 4. 종합 결론

1. **양자화 그룹 기반 방어(제안 방식)는 원본 RSR과 동일한 평균 정보량(keep_ratio)에서도 clean accuracy가 유의하게 낮다** — 개별 성분 단위의 정교한 확률적 선택이 그룹 단위의 거친 선택보다 정보 보존 효율이 높다는 뜻.
2. **적응형 공격 평가 방법론이 결론을 뒤집을 수 있다**: BPDA(단일 샘플 근사)는 무작위 방어의 취약성을 체계적으로 과소평가했고, EOT로 교체하자 더 강한 공격이 됨이 모든 조건에서 재현됐다.
3. **"무작위성이 결정론적 기댓값보다 나은가"는 공격 강도(ε)에 의존하는 질문이다.** 바닥 효과가 없는 구간(ε=4/255)에서는 PGD 기준 무작위 방식이 결정론적 방식보다 일관되고 통계적으로 유의하게 더 견고하다(floor 전 구간 재현). 반면 강한 공격(ε=8/255)에서는 둘 다 붕괴해 차이가 무의미해지거나 순서가 뒤집힌다.
4. **그룹 세분화(K)는 clean-robust 트레이드오프의 또 다른 축이다**: keep_ratio를 고정해도 K를 늘리면 clean accuracy를 내주고 robust accuracy를 얻는다.
5. **모든 방어(원본 RSR 포함)는 방법론적으로 올바른 적응형 공격(EOT-PGD) 앞에서 결국 강건성이 매우 낮은 수준(수 %대)에 머문다** — obfuscated gradients 문제로부터 완전히 자유롭지 않음. 다만 완전히 0%로 붕괴하지는 않으며, 그 잔여 강건성의 크기와 유의성이 무작위성 유무에 따라 갈린다는 것이 이 프로젝트의 핵심 실증 기여.

## 5. 방법론적 교훈

- **쌍체 데이터에는 McNemar 검정이 이표본비율 z-검정보다 검정력이 높고 더 적절하다** — 같은 1000장을 방식만 바꿔 비교하는 이 실험 설계에서 z-검정은 상관관계를 무시해 과소평가하는 경우가 관찰됨(floor=0.05 FGSM: McNemar는 유의, z-검정은 비유의).
- **단일 ε 값의 결과만으로 일반화하면 안 된다** — 바닥 효과(모든 방식이 붕괴)가 있는 구간에서 나온 결론은 반대 방향으로 뒤집힐 수 있다.
- **가설이 반박되는 실험도 유의미한 정보다** — K 세분화가 격차를 줄일 것이라는 가설은 반박됐지만, 그 결과가 스윕(B)에서 "K가 clean-robust 트레이드오프의 새로운 축"이라는 더 일반적인 발견으로 이어짐.

## 6. 한계 및 향후 과제

- 공격은 L∞ FGSM/PGD로 한정됨 — CW, AutoAttack 등 더 강력하거나 다른 노름(L2)의 공격에 대한 평가는 미수행.
- eot_samples는 K=5/floor=0.05 조건에서만 스윕함 — 다른 (K, floor) 조합에서도 N=10이 충분한지는 미검증.
- K=16의 결정론적 대조군은 계산량(2^16=65536 조합) 문제로 생략됨 — 근사(몬테카를로 기댓값 등)를 통한 대체 측정이 가능할지는 미탐색.
- CIFAR-10 · VGG16 단일 조합에서만 검증됨 — 다른 데이터셋/아키텍처에 대한 일반화는 확인되지 않음.
